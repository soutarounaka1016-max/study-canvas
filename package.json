{
  "name": "study-canvas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run check",
    "check": "node scripts/check.mjs",
    "test": "node --test"
  },
  "engines": { "node": ">=20" }
}
