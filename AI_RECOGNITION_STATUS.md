# AI_RECOGNITION_STATUS

更新日: 2026-07-28

## 目的

科目別の週間目標キャンバスをCloudflare Workers AIで読み取り、複数の勉強タスク候補へ変換し、利用者が確認・修正した項目だけを週間カードとして保存する。

## 実装済み

- 数学・英語・物理・化学・その他の表示中キャンバスをPNGへ変換
- 利用者が「AIで読み取る」を押した時だけCloudflare Workerへ送信
- WorkerからWorkers AI binding `AI`を呼び出す
- 主系モデルを`@cf/moondream/moondream3.1-9B-A2B`とし、短いOCR専用指示で画像内の文字だけを高速転記
- Moondreamの結果に日本語文字が含まれる場合は、画像と一次OCR結果を`@cf/google/gemma-4-26b-a4b-it`へ渡して再確認
- 一次OCR結果は参考情報として扱い、Gemmaには画像を唯一の正として誤字を訂正させる
- 英数字だけの結果はMoondreamを採用し、日本語はGemma補正後の結果だけを採用
- 科目名や「週間目標」などの見出し、指示文の復唱、空の結果をタスク候補として保存しない
- 主系8秒、補助10秒、合計最大18秒で終了し、時間内に使える結果がなければ既存カードを変更しない
- 科目をアプリ側で固定し、複数タスク候補を返す
- JSON Schema、OpenAI互換`choices`、推論欄、content配列を再帰的に解析
- JSON形式が守られない場合は、最終回答の箇条書きを確認付き候補へ安全に補正
- 読み取り候補の表示、内容修正、選択解除、削除、手動追加
- 選択した候補だけ週間カードとして保存
- 週間カードを週・科目ごとに`study-canvas:weekly-cards:v1`へ保存
- 再読み込み後に週間カードを復元
- 週間カードの削除
- 週間カードを統合バックアップへ追加し、項目別復元に対応
- 旧統合バックアップでは週間カードを上書きしない互換処理
- AI通信失敗、タイムアウト、形式不正では既存カードを変更しない
- localStorage保存確認に失敗した場合は以前の値へロールバック
- WorkerのCORSを正式なGitHub Pages公開元へ限定
- Worker応答を`Cache-Control: no-store`に設定

## キャッシュ事故と本番試験の再発防止

- `index.html`の公開識別子を`20260728-weekly-ai-cache-1`へ更新
- `release-entry.js`の参照を`release-entry.js?v=20260728-1`へ更新
- 公開入口より新しい日付の子モジュールを読み込む場合は静的テストを失敗させる
- GitHub Pages公開後に、公開HTML、公開入口JavaScript、週間AI JavaScriptを`Cache-Control: no-cache`で順番に取得する
- 公開版の実DOMで「AIで読み取る」「候補を手動追加」「選択した候補をカード化」が存在し、旧「準備中」表示が残っていないことを確認する
- 外部AI画像認識を変更した場合は、`/health`だけでなく本番Workerへ合成PNGを送る実動試験を必須とする
- 英字画像はMoondream主系、日本語画像はGemma補正経路を通ることを別々に検証する
- HTTP 200だけで成功扱いにせず、返却された候補に期待する勉強内容が含まれることまで確認する
- 一時的なWorker反映待ちに備え、本番認識試験は同じ条件で最大3回まで再試行する

## 自動確認済み

### 通常テスト

PR #58のGitHub Actionsで次を確認した。

- 単体テスト成功
- ビルド成功
- Chromium成功
- WebKit成功
- iPad縦向き相当成功
- iPad横向き相当成功
- 英字OCRはMoondream主系の結果を採用
- 日本語を含む一次OCRは画像とOCRヒントをGemmaで再確認
- 指示文復唱は候補として採用せずGemma補助へ切り替える
- 複数候補の表示、修正、選択保存
- 未選択候補が保存されない
- 再読み込み後もカードが残る
- AI失敗後も保存済みlocalStorageが変わらない
- 週間カード保存失敗時のロールバック
- 統合バックアップで週間カードを保存・復元
- JSONではない箇条書きを確認付き候補へ補正

### 本番公開確認

mainコミット`b7a95f34c74d8318f68e0ba8a6afab885d59501f`、GitHub Pages実行`30325144478`で次を確認した。

- GitHub Pagesデプロイ成功
- 本番Workerの`/health`成功
- 主系が`@cf/moondream/moondream3.1-9B-A2B`、補助が`@cf/google/gemma-4-26b-a4b-it`であることを確認
- 公開HTMLから最新版の公開入口・週間AIモジュールまでの取得連鎖成功
- 本番Workerへ英字の合成PNGを送信し、Moondream主系から期待内容を含む候補を取得
- 続けて日本語の合成PNGを送信し、Gemma補正経路から「微積分」または「チョイス」に一致する候補を取得
- 両方の応答で科目、週開始日、使用モデル、補助利用有無、応答時間、非空候補を検査
- 公開版Study CanvasをChromiumで開き、AI読み取り画面と重大なJavaScriptエラーの不在を確認
- 公開確認全体が成功状態で終了

本番試験は、読みやすい英字と日本語を描画した合成PNGによる経路・内容確認である。利用者本人の自由手書きに対する精度を保証する試験ではない。

## 外部送信

送信するのは、現在開いている1科目分の週間目標PNG、科目名、対象週の開始日だけである。日別キャンバス、他科目、自由ノート、既存カード、バックアップ内容は送らない。

画像は利用者が「AIで読み取る」を押した時だけ送信する。AIの読み取りだけでは保存データを変更せず、利用者が候補を確認して「選択した候補をカード化」を押した時だけ保存する。

## 実機未確認

- 学校支給iPad Safariで、利用者本人の日本語手書き画像を送った場合の認識精度
- 参考書名、漢字、ページ番号、問題数を含む実際の文字の誤読率
- 学校や自宅の回線での実際の応答時間
- 無料枠を日常利用した場合の利用回数と上限到達頻度
- Apple Pencilで書いた直後から候補修正・カード化までの体感操作性

## 完成判定

第一版の完成条件である、画像送信、Workers AI呼び出し、英字主系、日本語補正、候補取得、確認・修正、選択カード化、保存、再読み込み、バックアップ、失敗時データ保護、キャッシュ更新、通常ブラウザ確認、公開URL確認、本番Workerへの英字・日本語連続PNG実動試験はすべて満たした。

したがって、週間目標AI読み取り機能の第一版は自動確認上完成とする。残るのは利用者本人の日本語手書きに対する実用精度と、学校支給iPadでの体感確認であり、不具合修正ではなく実機利用評価として区別する。AIが誤読した場合でも、確認画面で修正してから保存するため、誤読だけで既存データが変更されることはない。
