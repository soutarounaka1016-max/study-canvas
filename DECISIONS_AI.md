# AI手書き認識の設計判断

更新日: 2026-07-28

## Cloudflare Workers AIを正式構成とする

- ブラウザからCloudflare Workerへ画像を送信し、WorkerからWorkers AI binding `AI`を呼び出す。
- ブラウザへAPIキーやCloudflare認証情報を保存しない。
- ブラウザからのアクセス元は`https://soutarounaka1016-max.github.io`へ限定する。
- Workerは画像や認識結果を保存せず、応答へ`Cache-Control: no-store`を付ける。
- Gemini APIキーを使う旧構成と、Tesseract.jsによる端末内OCRは正式構成として使用しない。

## 主系と日本語補正を分ける

- 主系モデルは`@cf/moondream/moondream3.1-9B-A2B`とする。
- Moondreamには長いJSON生成指示を与えず、画像内の文字だけを元の言語・改行のまま転記する短いOCR指示を与える。
- 英数字だけの読み取り結果は、速度を優先してMoondreamの結果を採用する。
- 一次OCR結果に日本語文字が含まれる場合は、画像と一次OCR結果を`@cf/google/gemma-4-26b-a4b-it`へ渡して再確認する。
- 一次OCR結果は誤字を含む参考情報として扱い、Gemmaには画像を唯一の正として訂正させる。
- 科目名や「週間目標」などの見出し、指示文の復唱、空の結果はタスク候補として採用しない。
- 日本語補正に失敗した場合は、誤読候補を保存せずエラーとして停止する。

## 追加費用を発生させない

- CloudflareはFreeプランのまま使用する。
- 主系・補助ともWorkers AI内のモデルだけを使用し、有料APIへの自動切替は行わない。
- `/health`は`noPaidFallback: true`を返し、無料構成であることを本番確認する。
- 無料枠または利用上限へ達した場合は`429 FREE_TIER_LIMIT`で停止する。
- 主系は8秒、補助は10秒、合計最大18秒で終了し、無制限の再試行を行わない。

## AIへ送る範囲を限定する

- 利用者が表示している1科目分の週間目標PNGだけを、「AIで読み取る」を押した時に送信する。
- PNGと一緒に送るのは、固定した科目名と対象週の開始日だけとする。
- 日別キャンバス、他科目、自由ノート、既存カード、バックアップ内容は送信しない。
- AIの読み取りだけではlocalStorageを変更しない。

## AIは入力補助に限定する

- AIは勉強内容の候補を返すだけで、予定時間、優先順位、今日の予定を勝手に決めない。
- 利用者は候補を修正、選択解除、削除、手動追加できる。
- 利用者が「選択した候補をカード化」を押した項目だけを保存する。
- 週間目標から今日の予定を自動生成・変更しない。
- AIが誤読しても、確認画面を経由せず既存データが変更されることはない。

## 失敗時は既存データを守る

- 通信失敗、タイムアウト、利用上限、形式不正、空の結果では既存カードを変更しない。
- localStorageへの保存確認に失敗した場合は、保存前の値へロールバックする。
- 旧バックアップを復元する場合、含まれていない週間AIカードを上書きしない。

## 本番確認を完成条件に含める

- `/health`だけでは完成扱いにしない。
- AI認識を変更した場合は、本番Workerへ合成PNGを実際に送信する。
- 英字画像はMoondream主系、日本語画像はGemma補正経路を通ることを別々に検証する。
- HTTP状態だけでなく、返却候補に期待する勉強内容が含まれることを検査する。
- GitHub Pages公開後に、公開HTML、入口JavaScript、対象モジュールを`no-cache`で取得してキャッシュ連鎖を確認する。
- 公開版をChromiumで開き、重大なJavaScriptエラーと旧UIの残存がないことを確認する。
- 自動確認済みと学校支給iPadでの実機未確認を管理ファイル上で分けて記録する。

## 現在の完成判定

mainコミット`b7a95f34c74d8318f68e0ba8a6afab885d59501f`のGitHub Pages実行`30325144478`で、英字画像のMoondream主系認識、日本語画像のGemma補正認識、公開URL、キャッシュ連鎖、公開版Chromium確認が成功した。

週間目標AI読み取り機能の第一版は自動確認上完成とする。残る本人の日本語手書き精度と学校支給iPadでの体感速度は、完成後の実機利用評価として扱う。
